version: "3.3"

services:
  whoami:
    image: marcellodesales/whoami
    build:
      context: .
      args:
        GIT_SHA: ${GIT_SHA:-master}
    ports:
      - 8000:8000/tcp
    environment:
      ENV: prod
